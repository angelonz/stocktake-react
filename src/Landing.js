import React from 'react';
import Banner from './Banner';
import Section from './Section';

const Landing = () => {
    return (
        <div>
            <Banner />
            <Section />
        </div>
    );
};

export default Landing;
